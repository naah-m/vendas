CREATE TABLE cliente (
    cpf      VARCHAR(255) NOT NULL,
    nome     VARCHAR(255),
    cep      VARCHAR(255),
    numero   VARCHAR(255),
    completo VARCHAR(255),
    telefone VARCHAR(255),
    PRIMARY KEY (cpf)
);